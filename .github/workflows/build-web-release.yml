name: Build Web Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-web-release-${{ github.ref_name || github.run_id }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  PNPM_VERSION: 10.10.0

jobs:
  build-web-windows:
    name: Build Web (Windows x86_64)
    runs-on: windows-2022

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Enable Corepack
        shell: bash
        run: |
          corepack enable
          corepack prepare pnpm@${PNPM_VERSION} --activate
          pnpm --version

      - name: Setup pnpm cache
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Resolve release metadata
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="$(node -p "require('./package.json').version")"
          fi

          ASSET_NAME="cli-memory-web-v${VERSION}-windows-x86_64.exe"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "ASSET_NAME=$ASSET_NAME" >> "$GITHUB_ENV"

      - name: Install frontend dependencies
        shell: bash
        run: pnpm install --frozen-lockfile

      - name: Install Chromium for smoke check
        shell: bash
        run: pnpm exec playwright install chromium

      - name: Build frontend (Web mode)
        shell: bash
        run: pnpm build:web

      - name: Verify frontend build
        shell: bash
        run: |
          test -f dist/index.html
          ls -la dist/

      - name: Build backend (Release mode)
        shell: bash
        run: cargo build --release --manifest-path crates/server/Cargo.toml

      - name: Smoke test Web release binary
        shell: bash
        run: pnpm smoke:web-release -- --binary crates/server/target/release/cli-memory.exe

      - name: Create release asset
        shell: bash
        run: |
          mkdir -p release-assets
          cp crates/server/target/release/cli-memory.exe "release-assets/${ASSET_NAME}"
          ls -lah release-assets/

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64
          path: release-assets/
          retention-days: 7

  build-web-linux:
    name: Build Web (Linux x86_64 (Ubuntu 20.04))
    runs-on: ubuntu-22.04
    container:
      image: ubuntu:20.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Linux build dependencies
        shell: bash
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            build-essential \
            ca-certificates \
            curl \
            git \
            pkg-config \
            libssl-dev

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Enable Corepack
        shell: bash
        run: |
          corepack enable
          corepack prepare pnpm@${PNPM_VERSION} --activate
          pnpm --version

      - name: Setup pnpm cache
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Resolve release metadata
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="$(node -p "require('./package.json').version")"
          fi

          ASSET_NAME="cli-memory-web-v${VERSION}-linux-x86_64-ubuntu20.04"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "ASSET_NAME=$ASSET_NAME" >> "$GITHUB_ENV"

      - name: Install frontend dependencies
        shell: bash
        run: pnpm install --frozen-lockfile

      - name: Install Chromium for smoke check
        shell: bash
        run: pnpm exec playwright install --with-deps chromium

      - name: Build frontend (Web mode)
        shell: bash
        run: pnpm build:web

      - name: Verify frontend build
        shell: bash
        run: |
          test -f dist/index.html
          ls -la dist/

      - name: Build backend (Release mode)
        shell: bash
        run: cargo build --release --manifest-path crates/server/Cargo.toml

      - name: Smoke test Web release binary
        shell: bash
        run: pnpm smoke:web-release -- --binary crates/server/target/release/cli-memory

      - name: Create release asset
        shell: bash
        run: |
          mkdir -p release-assets
          cp crates/server/target/release/cli-memory "release-assets/${ASSET_NAME}"
          chmod +x "release-assets/${ASSET_NAME}"
          ls -lah release-assets/

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-x86_64-ubuntu20.04
          path: release-assets/
          retention-days: 7

  release:
    name: Publish Web release assets
    if: startsWith(github.ref, 'refs/tags/')
    needs:
      - build-web-windows
      - build-web-linux
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Restore executable bits
        shell: bash
        run: |
          chmod +x release-assets/cli-memory-web-v${GITHUB_REF_NAME#v}-linux-x86_64-ubuntu20.04
          ls -lah release-assets/

      - name: Create release notes
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          {
            printf '## CLI Memory Web v%s\n\n' "$VERSION"
            printf '%s\n\n' 'Official GitHub Releases now publish the Web runtime only.'
            printf '%s\n' '### Assets'
            printf -- '- Windows x86_64: `cli-memory-web-v%s-windows-x86_64.exe`\n' "$VERSION"
            printf -- '- Linux x86_64 baseline: `cli-memory-web-v%s-linux-x86_64-ubuntu20.04` (built in an Ubuntu 20.04 container on an ubuntu-22.04 runner)\n\n' "$VERSION"
            printf '%s\n' '### Quick start'
            printf -- '- Windows: run `cli-memory-web-v%s-windows-x86_64.exe`\n' "$VERSION"
            printf -- '- Linux: `chmod +x ./cli-memory-web-v%s-linux-x86_64-ubuntu20.04 && ./cli-memory-web-v%s-linux-x86_64-ubuntu20.04`\n\n' "$VERSION" "$VERSION"
            printf '%s\n' '### Runtime configuration'
            printf '%s\n' '- Default address: `http://127.0.0.1:17666`'
            printf '%s\n' '- Override port: `CLI_MEMORY_PORT=8080`'
            printf '%s\n\n' '- Override host: `CLI_MEMORY_HOST=0.0.0.0`'
            printf '%s\n' 'Desktop Tauri packages are not part of this release.'
          } > release-notes.md

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          files: release-assets/*
          body_path: release-notes.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
