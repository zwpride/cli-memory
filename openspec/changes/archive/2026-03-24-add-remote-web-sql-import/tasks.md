## 1. Web Import Backend

- [x] 1.1 为 Web runtime 增加同源且受认证保护的 SQL 上传入口，避免远程浏览器依赖服务器本机文件路径
- [x] 1.2 抽出桌面路径导入与 Web 上传导入共享的导入执行逻辑，保留现有 SQL 格式校验、安全备份与导入后同步语义
- [x] 1.3 为上传入口补充最小回归覆盖，确认未认证请求被拒绝且导入失败不会污染现有数据库

## 2. Settings UI Split

- [x] 2.1 拆分设置页导入状态与 API，使桌面端继续使用文件路径导入、Web runtime 改为浏览器文件上传
- [x] 2.2 更新导入交互与状态提示，确保 Web 模式不再暴露桌面端文件对话框假设，且桌面端现有导入导出体验不回退

## 3. Remote Access Contract

- [x] 3.1 收紧 Web runtime 在非回环 host 场景下的端口行为，确保请求端口冲突时明确失败而不是静默换端口
- [x] 3.2 更新 Web 模式文档与启动提示，说明远程访问需要的 host、固定端口与认证前提
- [x] 3.3 记录并执行最小验证集合，覆盖桌面端路径导入、Web 本地上传以及远程访问场景下的端口稳定性

## 4. Web Export Download

- [x] 4.1 为 Web runtime 增加同源且受认证保护的 SQL 下载入口，使浏览器可以直接下载导出文件而不依赖服务器本机保存路径
- [x] 4.2 抽出桌面路径导出与 Web 浏览器下载共享的导出执行逻辑，保持现有 SQL 导出格式与文件命名语义一致
- [x] 4.3 更新设置页导出交互与状态提示，使桌面端继续使用保存对话框、Web runtime 改为浏览器下载
- [x] 4.4 为下载入口补充最小回归覆盖，确认未认证请求被拒绝、响应头可触发浏览器下载且桌面端导出不回退
- [x] 4.5 更新 Web 模式文档与验证记录，说明浏览器下载导出路径以及远程访问场景下的认证前提
